<form method="post" action="{{ path('app_page_admin_delete', {'idp': page.idp}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.idp) }}">
    <button class="btn">Supprimer</button>
</form>
