<form method="post" action="{{ path('app_Super_admin_delete', {'id_P': publication.id_P}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication.id_P) }}">
    <button class="btn">Supprimer</button>
</form>
